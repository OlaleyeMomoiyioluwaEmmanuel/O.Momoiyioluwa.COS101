
// Rust program to count numbers

use std::10;

fn main(){

printini("Enter lower bound"); Let mut input1 - String::new(); 10::stdin().readLine(Raut input1).expect("Failed to read input"); Let lower bound: 132-input1.trim().parse().expect("Failed to input

printini("Enter upper bound"); Let aut input2-String::new(); 10::stdin().readLine(Raut input2).expect("Failed to read input"); Let upper bound: 132 input2.trim().parse().expect("Failed to input"

for % in lower bound upper bound // upper bound is not inclusive

printini("Count Level is ()",x);

}

